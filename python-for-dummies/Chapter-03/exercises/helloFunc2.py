def hello(name="default"): # defines a function, hello with a parameter name
    print("Hello, " + name)

hello("Alice") # passes "Alice" as the name
hello("Bob") # passes "Bob" as the name
hello()  # uses the default name